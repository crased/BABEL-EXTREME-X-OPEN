"""Runtime configuration dataclasses (scaffold).

Planned fields: input_path, output_dir, page_range, dpi, ocr_engine,
ocr_lang, confidence_threshold, layout_backend.
"""

# TODO(phase-0): define @dataclass(frozen=True) PipelineConfig.
