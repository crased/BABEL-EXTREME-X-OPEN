"""OCR adapter test (scaffold). Phase-3 task 3.1.

Feed a tiny synthetic image with known text; assert substring + confidence > 0.
"""

# TODO(phase-3): generate a PIL image with a known Russian phrase via PIL.ImageDraw.
